# 프론트 (Vite) — Vercel env (선택, 미설정 시 아래 Render URL 자동 사용)
# VITE_API_BASE_URL=https://two0260531-remario.onrender.com

# 백엔드 (로컬 — backend/data/ 에 DB·백업 JSON 영구 저장)
# DATA_DIR=backend/data
# DATABASE_PATH=backend/data/mario.db
# CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# 백엔드 (Render — PostgreSQL + /data 디스크 백업, render.yaml 참고)
# DATABASE_URL=postgresql://...
# DATA_DIR=/data
